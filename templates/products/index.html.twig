{% extends 'base.html.twig' %}

{% block title %}Products{% endblock %}

{% block body %}
<div class="container d-flex flex-wrap my-3">
  <form>
    <button type="button submit" name="filter" value="all" class="btn btn-info m-3">ALL</button>
  </form>
  {% for category in categories %}
    <form>
      <button type="button submit" name="filter" value={{category.id}} class="btn btn-info m-3">{{category.name}}</button>
    </form>
  {% endfor %}
</div>

<div class="container">
  <div class="row">
    {% for product in products %}
        <div class="col-sm-6 col-md-4 my-2">
          <div class="card text-white bg-dark" >
            <div class="card-header">{{ product.category }}</div>
            <div class="card-body">
                <img src={{ product.image }} alt={{ product.name }} class="w-100"/>
                <h4 class="card-title my-3">{{ product.name }}</h4>
                <p class="card-text">Price : {{ product.price }} €</p>
                <p class="card-text">{{ product.description }}</p>
                <a href="{{path('app_cart_add_Products', {'id' : product.id} )}}">
                  <button class="btn btn-success">+ Add to cart</button>
                </a>
            </div>
          </div>
        </div>
    {% endfor %}
  </div>
</div>
{% endblock %}
